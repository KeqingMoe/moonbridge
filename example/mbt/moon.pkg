options(
  link: {
    "native": {
      "cc": "clang",
      "cc-link-flags": "-lc++ -L../../build -lexample",
    },
  },
  "supported-targets": [ "native" ],
  targets: { "mbt.mbt": [ "native" ] },
)
